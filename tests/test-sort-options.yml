name: Test sort options
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          token: ${{ github.token }}
          org: ${{ github.repository_owner }}
          days: '7'
          sort: stars
          sort-order: asc
          stale: '30'
          old: '180'
          json: 'false'
          diff-report: 'false'
          committer-name: github-actions
          committer-email: github-actions@github.com
      - name: Assert action ran with custom sort options
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "Action outcome: $OUTCOME"
          # The action makes real GitHub API calls and pushes to the repo.
          # It may fail if the owner is a personal account (not an org) or
          # if rate limits are hit. We accept success or failure here as a
          # smoke test that the action can be invoked with sort=stars sort-order=asc.
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: Action was invoked and ran with custom sort options (outcome: $OUTCOME)"
          else
            echo "FAIL: Unexpected outcome: $OUTCOME"
            exit 1
          fi
